client localhost {
        ipaddr = 127.0.0.1
        proto = *
        secret = testing123
        limit {
                max_connections = 16
                lifetime = 0
                idle_timeout = 900
        }
}
